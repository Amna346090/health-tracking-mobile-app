/**
 * Staff-role management UI (Manage Users, provider assignment) is hidden while
 * the client runs with a single admin account. Backend + components stay
 * intact — flip this back on if staff accounts are reintroduced.
 */
export const STAFF_FEATURES_ENABLED = false;
